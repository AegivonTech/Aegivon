{
  "name": "aegivon-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend && npm run build --prefix admin",
    "install:all": "npm i --prefix backend && npm i --prefix frontend && npm i --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
